# research/ layer pinned dependencies (build contract §0).
# These are the RESEARCH deps ONLY. Never add them to the ingest service's
# minimal app/ deps. Pinned to the hmog_1dcnn conda env (Python 3.10, CPU torch).
numpy==1.26.4
torch==2.4.1
pandas==2.2.2
scikit-learn==1.5.2
scipy==1.13.1
matplotlib==3.9.2
pyarrow==17.0.0
pyyaml==6.0.3
pytest==9.0.3
lz4>=4.3
