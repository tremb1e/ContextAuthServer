# M0 sensor_only_dense (spec §9.4 / runner.M_OVERRIDES["m0"]).
# 3-channel IMU only, plain dense encoder. RQ1 lower bound (no UI structure).
# Thin override of configs/default.yaml (deep-merged over it by config.load_config).
model:
  kind: dense
  hidden_dims: [128, 64]
features:
  mode: sensor_only
